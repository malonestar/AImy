# BERT Base Uncased

This directory contains tokenizer files for the `bert-base-uncased` model.

These files are typically downloaded automatically via Hugging Face
and are intentionally excluded from Git.

If this directory is empty except for this README, the model will be
re-downloaded at runtime or during setup.
